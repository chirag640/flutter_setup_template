import 'package:flutter/material.dart';

/// App brand color palette
class AppColors {
	// Primary (navy) and secondary (gold) inspired by the mockup
	static const Color primary = Color(0xFF0C2A5B); // deep navy
	static const Color primaryDark = Color(0xFF081D41);
	static const Color secondary = Color(0xFFF4B400); // golden accent
	static const Color secondaryDark = Color(0xFFB58000);

	// Neutrals
	static const Color surface = Color(0xFFF5F6FA);
	static const Color surfaceDark = Color(0xFF121212);
	static const Color background = Color(0xFFF2F3F7);
	static const Color backgroundDark = Color(0xFF0E0E0E);
	static const Color outline = Color(0xFFCFD3DA);
	static const Color textPrimary = Color(0xFF0B0B0B);
	static const Color textSecondary = Color(0xFF5F6B7A);

	// Status
	static const Color success = Color(0xFF2E7D32);
	static const Color warning = Color(0xFFF9A825);
	static const Color error = Color(0xFFB00020);
}
